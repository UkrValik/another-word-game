import { Text } from 'react-native';

import { ScreenWrapper } from '../common/components/screen-wrapper';

export const ArchivScreen = () => {
  return (
    <ScreenWrapper>
      <Text style={{ fontSize: 30 }}>Archiv Screen</Text>
    </ScreenWrapper>
  );
};
